# 美术素材需求 · ART REQUIREMENTS

**2026-07-23 按实测重写。** 每条素材标注四种状态之一，由 `tools/art/audit.py` 机器判定，不靠记忆：

| 状态 | 含义 |
|---|---|
| ✅ **完好** | 文件存在、可解码、无缺陷 |
| ⚠️ **棋盘格** | 编辑器的透明棋盘被烘焙成实际像素（alpha 通道本身正确，粗查发现不了） |
| ❌ **损坏** | 无法解码／全透明／尺寸退化 |
| **已接线** | `game/` `core/` `content/` 中确有引用 |

```bash
python3 tools/art/audit.py           # 汇总
python3 tools/art/audit.py --md      # 需关注项的表格
python3 tools/art/audit.py --unused  # 未接线清单
python3 tools/art/strip_checker.py --write --glob 'assets/art/*.webp'   # 修棋盘格
```

---

## 0. 实测汇总

```
总数 297 张 · ✅ 完好 297 · ⚠️ 棋盘格 0 · ❌ 损坏 0
已接线 13 · 未接线 284
```

**2026-07-23 修复记录**：

| 缺陷 | 数量 | 处理 |
|---|---|---|
| 棋盘格烘焙进像素 | **169** | `strip_checker.py` 全量清除并目视复核 |
| 路线笔触墨迹仅占画布 7–12/96 行，拉伸后不可见 | 3 | 裁到墨迹范围 |

> ⚠️ **生成管线未修**：新素材仍会带棋盘格。**每次生成后必须跑 `audit.py`。**

---

## 1. 内容错配（需重绘，非缺失）

| 素材 | 问题 | 现状 |
|---|---|---|
| `scene-region-isl.webp` | **画的是中国湖景**（灯笼、画舫、拱桥），不是伊斯兰世界 | 已由 `MapArt.CITY_SCENE` 优先取具名场景绕开 |
| `scene-region-chr.webp` | 画的是沙漠商队，更像中亚而非拉丁基督教世界 | 同上 |

这两张是**内容错误不是技术缺陷**——文件完好，画错了对象。机器查不出来，只能人看。

---

## 2. 仍缺的素材（按阻塞程度）

| # | 素材 | 数量 | 规格 | 阻塞 |
|---|---|---|---|---|
| 2.1 | **六主城场景图** | **6** | 1920×1080 WebP，暖褐低饱和 | ⛔ 走廊中段必经，现用 band 底板顶替 |
| 2.2 | band 场景底板重绘 | 2 | 同上 | ⛔ §1 的两张 |
| 2.3 | 罗盘玫瑰 | 1 | 带方位射线 | 地图图层唯一未交付项 |
| 2.4 | 签筒／签条 | 2–3 | — | 签占已可玩，用占位 |
| 2.5 | 命运九等徽记 | 9 | — | P3 命格显示 |
| 2.6 | 史料小卡样式 | 1 | — | GDD §19 红线，现用文字标注 |
| 2.7 | 职业 NPC 立绘 | 36 | 9 职业 × 4 文化 | P5 随从 |
| 2.8 | 货格／库存格 | 少量 | — | P4 交易 |

### 2.1 六主城明细

| 城 | 画面提示（据原文） | 文件名 |
|---|---|---|
| 报达 baldacum | 底格里斯河畔大城，哈里发宝库与织金锦市 | `scene-baldacum-river.webp` |
| 巴里黑 balc | 曾经宏大今半毁，大理石宫殿残迹 | `scene-balc-ruins.webp` |
| 撒马尔罕 samarcanda | 果园环绕，蓝釉陶砖 | `scene-samarcanda-orchard.webp` |
| 可失合儿 cascar | 南北道分岔的绿洲市镇，商队分道 | `scene-cascar-crossroads.webp` |
| 于阗 cotan | 玉河、棉田、葡萄园 | `scene-cotan-jaderiver.webp` |
| 罗卜 lop | 大沙漠边缘最后市镇，驼队补给 | `scene-lop-desertedge.webp` |

---

## 3. 故事与互动所需素材

城市探索（`game/screens/city_view.gd`）已用 `npc-{venue}-{set}` 16 张立绘，按场所与文化取图。要让 102 城不重样，需要：

| # | 素材 | 数量 | 说明 |
|---|---|---|---|
| 3.1 | 场所立绘扩充 | +16 | 现有 4 场所（inn/market/tea/temple）× 4 文化；再加**码头工／官吏／医者／书记**四类 |
| 3.2 | 物品弹窗图 | 约 60 | 商品各一，`ic-*` 143 张中应有可复用 |
| 3.3 | 探索点小图 | 36 | 12 主城 × 3，用于城市视图的地点标牌 |

> **3.2 优先**：143 张 `ic-*` 图标尚未接线，先做**图标↔商品 id 的映射表**再决定要不要新绘——很可能大部分能复用。

---

## 4. 已接线清单

| 素材 | 用途 |
|---|---|
| `map-vellum-tile` | 地图羊皮纸底 |
| `map-fog-ink` | 迷雾遮罩（`game/shaders/fog.gdshader`） |
| `map-city-{chr,con,isl,mazu}-{s,m,l}` 12 张 | 城塞小像，按文化与 tier |
| `map-mtn-*` 8 张 | 侧视山脉立面，按海拔取形 |
| `map-route-{land,sea,river}` | 路线笔触 |
| `map-wind-{n,e,s,w}` | 图幅四缘风神头 |
| `scene-*` 具名场景 10 张 | 城市探索背景 |
| `npc-{inn,market,tea,temple}-{4文化}` 16 张 | 城市探索立绘 |
| `scene-desk-opening` | 书案开场 |

---

## 5. 未接线的 284 张

| 前缀 | 数量 | 何时接 |
|---|---|---|
| `ic-*` | 143 | P4 市集与图鉴（见 §3.2） |
| `ui-*` | 40 | 界面美化 |
| `sym-*` | 35 | P3 占卜 UI |
| `map-*` | 27 | 地图润色（海怪、船、森林、礁石） |
| `npc-*` | 16 | 部分已用 |
| 其余 | 23 | — |

完整清单：`python3 tools/art/audit.py --unused`

---

## 6. 音频

详见 `AUDIO_PLAN.md`。现状：20 stem + 12 环境 + 5 sacred_blur，8.8 MB，A1–A6 全部落地。

**红线**：不得合成可辨识语义的礼拜声响；圣所只留空间感与偶发钟磬。上线前须与美术、文本一并送敏感读者审阅。
