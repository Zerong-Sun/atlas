# 当前文本生产需求 · TEXT REQUIREMENTS

**版本：2026-08-11 · FateQuest v3.0 · 当前权威文本需求文档**

本文只管理“现在还需要写什么、哪些文本已经完成、每段文本怎样接入和验收”。
已完成的 T1–T6 密度批次和旧版库存表已移入 `docs/archive/`，不再作为当前待办。

关联文档：

- 项目状态：[`STATUS.md`](STATUS.md)
- 当前执行计划：[`PLAN.md`](PLAN.md)
- 12 主城接线表：[`12_CITY_CLOSURE_MATRIX.md`](12_CITY_CLOSURE_MATRIX.md)
- 中英文流程与译者守则：[`L10N_PLAN.md`](L10N_PLAN.md)
- 文本格式：[`STORY_TEXT_FORMAT.md`](STORY_TEXT_FORMAT.md)
- 总体需求与 GDD 验收：[`FATEQUEST_ENGINE_REQUIREMENTS.md`](FATEQUEST_ENGINE_REQUIREMENTS.md) §10
- 历史基线：[`archive/TEXT_REQUIREMENTS_2026-07-31_BASELINE.md`](archive/TEXT_REQUIREMENTS_2026-07-31_BASELINE.md)

## 1. 当前结论

当前文本状态要分成三个层次理解：

| 层次 | 当前状态 | 说明 |
|---|---|---|
| 文本 key 是否存在 | ✅ 完成 | en/zh 各 5465 条，当前引用缺失 0（含事件授予的 codex/sticker、`ui.culture.<band>` 与代码字面键，G17 扩展覆盖） |
| 已有事件是否有反馈 | ✅ 完成 | 选择均有 `resultText` 或 `queue_event`（G29） |
| 每个互动是否都有多轮剧情 | ✅ 12 主城 + 21 city | 12 主城 site/mentor 与 21 city 深化（24 followup）已闭环；其余 site 仍可按 §4.3 继续质检 |

“文本已经写好”不能只看 i18n 数量。一个互动只有同时满足“文本存在、选择接线、效果有明确叙述、玩家能继续推进”时，才算完成。

## 2. 当前实测基线

数据来源：`content/tables/events/`、`content/story/`、`content/i18n/`、`tools/validate/validate.mjs`。

| 指标 | 当前数量 | 状态 |
|---|---:|---|
| 事件 | 429 | ✅ 已接入运行时 |
| 双语 i18n 条目 | en/zh 各 5465 | ✅ 缺失 0 |
| 故事 authoring 条目 | 3763 current | ✅ 0 stale、0 missing |
| site 事件 | 93 | ✅ 12 主城 + 21 city 已多轮深化 |
| 12 主城 site | 36 | ✅ followup 已齐 |
| 12 主城 mentor | 12 | ✅ followup 已齐 |
| 21 city 深化 followup | 24 | ✅ G31 + smoke |
| 12 主城入口后果链 | 24 条重要分支、48 个后果页 | ✅ 完成 |

当前“真正有下一页事件”的起始点为 13 个：12 个主城入口事件，加上 `ev-tauris-a` 的外国商人后续。故不能把 332 个事件或 3179 个文本 key 直接理解成 332 个完整多轮故事。

## 3. 已完成内容（归档，不重复开工）

以下内容已经完成，只保留作基线和回归依据：

| 批次 / 模块 | 已完成内容 | 验收依据 |
|---|---|---|
| T1 | 20 城入城正文改写与文化校订 | G24、故事检查 |
| T2 | 第二批占卜拜师、接线和结果文本 | 占卜测试、双语检查 |
| T3 | 21 座 city 各 2 个探索点，共 42 个 site | G26、城市导航 smoke |
| T4 | 法德兰草原途中事件与 steppe 文本 | G27、`test_m1_lines` |
| T5 | 文本审计遗留项 | i18n、事件和逻辑测试 |
| T6 | 19 城弱证据/查无语料处置 | `origin`、`disposition`、`note` |
| B1–B4 | 现有英文文本的中文翻译 | en/zh 双语校验、G21 |
| M3 | 白图泰六城与相关结局文本 | 主流程、语料接线 |
| M3b ✅ 2026-08-11 | 白图泰《里哈拉》全量接线：新增马儿代夫城市节点与入城文本（`ev-maldive-entry`）、13 个探索事件（西亚 7 + 海路 5 + 马儿代夫 1，含德里双事件；刺桐/行在/古里/没来 4 个由入城选择 `queue_event` 触发）、17 城 `altRefs` 二次声部、`battuta` archetype（丹吉尔式旅人，亚历山大出发朝觐麦加） | G17/G24/G26/G31、`story.mjs check`、smoke 套件 |
| zancibar 说明 | 桑给巴尔无 Lee 节译语料（语料全文零命中），本轮不新增节点，保持 ART_REQUIREMENTS 规划状态 | — |
| 无节点地区说明 | 白图泰语料另 12 place（丹吉尔/突尼斯/艾达卜/加沙/设拉子/马里/廷巴克图等）对应城市不在游戏世界范围，保持语料状态不接线 | — |
| 12 主城入口 | 每城前两项重要选择进入两页后果链 | G28、`12_CITY_CLOSURE_MATRIX.md` |
| 大不里士外国商人 | “向外国商人打听前路”进入后续追问事件 | `smoke_tauris_merchant.gd` |
| 运行时结果页 | `resultText` 在对话卡中显示，点击继续后返回城市 | 全部 smoke、城市导航回归 |

已完成内容的原始规格见：

- `docs/archive/TEXT_REQUIREMENTS_2026-07-31_BASELINE.md`
- `docs/archive/STORY_REQUIREMENTS_2026-07-26_T3-T6.md`
- `docs/archive/PLAN-2026-07-T1-T6.md`

## 4. 未完成需求总表

### 4.1 P0：12 主城探索点和导师的多轮互动

这是当前最重要的文本缺口，也是本轮用户反馈直接指出的范围。

12 主城共有 36 个探索点和 12 个导师互动，共 48 个城市内可交互对象。入口事件已有后果链，不重复计算。当前仅 1 个探索点拥有第二页，因此至少还有：

- 35 个主城探索点需要补第二页；
- 12 个主城导师互动需要明确是否补第二页；若纳入统一标准，则需要补 12 页；
- 最低新增目标：47 个“第一轮之后的后续事件页”。

每个 P0 互动至少实现：

1. 第一轮场景正文；
2. 3 个主要选择；
3. 至少 2 个选择进入第二轮对话或后果页；
4. 第二轮至少 2–3 个选择；
5. 每个选择都有物品、情报、故事、状态或明确行动结果；
6. 重要分支可继续 2–4 个事件页，普通分支至少完成一页反馈；
7. 关闭后返回城市时，已完成探索点状态正确刷新。

### 4.2 P1：21 座 city 的探索点深化 ✅ 2026-08-06

21 座 city 已有 42 个探索点并通过 G26。本批已把探索点升级为多轮互动：

- ✅ 每座 city 至少 1 个探索点做两轮互动（默认 `-a`）；
- ✅ 枢纽城（路线度 ≥5）`chamba` / `badashan` / `tanpiju` 两点都有第二页；
- ✅ 每个升级点含物品、情报或人物关系后果（见 [`21_CITY_DEEPENING_MATRIX.md`](21_CITY_DEEPENING_MATRIX.md)）；
- ✅ 城专属正文，无通用模板句；
- ✅ 门禁 **G31** + `tests/smoke_21city_followups.gd`。

生成器：`tools/lore/build_21city_followups.mjs` → `content/tables/events/followups_21city.json`。

### 4.3 P2：剩余已有 site 的反馈和文本质量

当前 93 个 site 中，除大不里士外国商人外，多数是一次选择后结算。后续生产时需要逐项检查：

- 选项是否真的有可见文本，而不是只改变数值；
- 获得的物品是否说明“得到什么、为何得到、下一步能做什么”；
- 揭示的城市/道路是否在结果文本中点名；
- 获得的图鉴/故事是否有可读正文，而不是只有 codex ID —— ✅ 2026-08-11：全部事件授予的 `codex.cx-*` 与 `sticker.*` 均有 name+body 双语正文（新单元 `content/story/codex/`、`content/story/stickers/`），缺失即 G17 报错；
- 一次性购买或观察行为是否需要二次追问；
- `once: true` 是否符合设计，不能因配置错误导致玩家无法再次阅读已完成故事。

### 4.4 P3：24 法正式课程和占法工具文本

当前 24 法已有数据和练习配置，但正式课程、工具说明、失败反馈和动画提示仍有运行占位。需要补齐：

- 方法标题、来源说明和学习目标；
- 课程正文与分步操作提示；
- 成功、失败、跳过和重试文本；
- 结果解释与可执行的旅行建议；
- 该占法如何影响路线、时间、物品或关系；
- 中英文术语表和文化复核记录。

### 4.5 P4：新增 UI 和发布文本

仍需正式化的文本包括：

- 事件结果页、后果暂停页和恢复提示的最终文案；
- 存档损坏、版本不兼容、自动存档失败等错误提示的统一语气；
- 课程、占法、地图和路线界面的说明文字；
- 200% 字号、英文长文本和键盘焦点下的可读性修订。

### 4.6 暂不纳入当前批次

- 102 城全部扩展为完整多轮主线：当前先完成 12 主城，再扩展 21 city；
- town / station 的完整探索点：当前数据模型不要求；
- 第三语言（日语、韩语、西班牙语）：保留流程可行性，不进入本批生产；
- 《远游记》《瀛涯胜览》拆解：✅ 2026-08-06（瀛涯书案＋二次声部；远游记 desk_only）
- 图鉴大规模扩写：作为可选加深，不阻塞本批多轮互动验收；
- 刺桐 wonder/市集/祠庙专属文案已入库未接线：`city.zayton.wonder`、`city.zayton.market.*`、`city.zayton.shrine.*`，留作主城专属 UI 素材。

## 5. 多轮互动文本规格

### 5.1 最低事件结构

每个需要多轮互动的对象至少由以下事件组成：

```text
第一轮事件
  ├─ 选择 A → 即时结果，或 queue_event → 第二轮事件
  ├─ 选择 B → 即时结果，或 queue_event → 第二轮事件
  └─ 选择 C → 轻量结果 + 下一步行动锚点

第二轮事件
  ├─ 选择 A → 物品 / 情报 / 关系后果
  ├─ 选择 B → 故事 / 图鉴 / 路线后果
  └─ 选择 C → 时间 / 风险 / 返回城市
```

重要故事可以继续到第三、第四页，但不得为了凑页数制造无意义的重复问答。

### 5.2 第一轮文本包

每个事件必须有：

| 文本 | 要求 |
|---|---|
| `title` | 点名地点、人物或事件，不用通用“城市探索” |
| `body` | 150–400 字英文源文；中文按语体等价翻译，不机械逐字对应 |
| 选择标签 | 3 个主要选择，动作明确，能看出差异 |
| 选择前置 | 钱、语言、占法、关系等限制必须写清原因 |
| `resultText` | 立即结算的选择必须说明变化和下一步 |
| `queue_event` | 需要继续剧情的选择必须指向有效事件 ID |
| `lore` | 标明 `source` / `authored` / `hybrid`，有来源时保留 ref |

### 5.3 后续事件文本包

后续事件不能只是重复第一轮正文。必须至少新增一种信息：

- 新人物发言或立场；
- 新的路线、城市、货物或风险信息；
- 新的物品交换、赠与、购买或损失；
- 新的图鉴/故事片段；
- 新的关系、声望或身份后果；
- 明确的行动锚点，例如“前往某城”“等待一天”“去找某人”。

### 5.4 结果文本和效果对齐

| 效果 | 文本必须说明 |
|---|---|
| `goods` | 物品名称、数量、获得/消耗原因、后续用途或限制 |
| `reveal_map` | 被揭示的城市或路线名称，以及为什么知道它 |
| `codex` | 获得的故事/知识主题，不能只说“图鉴更新” —— ✅ 2026-08-11 起正文与授予事件逐一对应，缺失即 G17 报错 |
| `sticker` | 贴纸代表的事件或纪念意义 —— ✅ 2026-08-11 起全部 60 张贴纸有正文，缺失即 G17 报错 |
| `reputation` | 哪一方对玩家的态度变化 |
| `days` | 等待或赶路的叙事原因和代价 |
| `coins` | 付款/报酬对象和交易原因 |
| `language` / `learn_divination` | 学习场景、用途和失败后果 |

每个效果至少满足一项：可见结果文本、后续事件、明确行动锚点。重要效果应同时满足两项以上。

## 6. 物品、故事、情报三类生产要求

### 6.1 物品型互动

物品不是单独显示一个名称就算完成。文本包必须包含：

1. 物品从谁处获得或购买；
2. 物品为何与当地场景有关；
3. 物品数量、价格或消耗条件；
4. 获得后的实际用途、货格影响或后续交易价值；
5. 中英文物品名、描述和结果反馈。

### 6.2 故事型互动

故事型互动必须产生可阅读内容：

1. 至少一段新正文，不得只复用城市简介；
2. 至少一个人物、地点或事件细节；
3. 选择后说明玩家带走了什么故事；
4. 如写入 codex，codex 正文必须与选择结果一致；
5. 对来源、改写和虚构部分标注 `lore.origin`。

### 6.3 情报型互动

情报必须能改变玩家下一步决策：

1. 点名城市、路线、关卡、水源、季节、语言或风险；
2. 明确情报从何而来；
3. 若揭示地图，`reveal_map` 的值必须和文本名称一致；
4. 给出行动锚点，例如去哪里、何时出发或需要准备什么；
5. 中英文不得出现不同地名、方向或数字。

## 7. 文本 authoring 与接线规范

文本唯一源文件：

```text
content/story/<unit>/en.md  ← 英文源文
content/story/<unit>/zh.md  ← 中文译文与校订
content/i18n/{en,zh}.json   ← story.mjs 编译产物，不直接手改
```

事件数据继续放在 `content/tables/events/`，不得在 UI 中硬编码剧情分支。每个新增后续事件必须：

- 使用稳定、可读、唯一的事件 ID；
- 由选择的 `queue_event` 接入；
- 在同一城市或有效范围内可触发；
- 所有选择带 `resultText` 或 `queue_event`；
- 所有 `effects` 带 `reason`；
- 中英文 key 同步存在；
- 重新运行生成器后仍可复现。

标准流程：

```bash
node tools/lore/story.mjs check
node tools/lore/story.mjs build
node tools/lore/story.mjs stamp zh
node tools/lore/story.mjs check
node tools/validate/validate.mjs --quiet
```

## 8. P0 文本接线矩阵

新建或维护矩阵时，每行代表一个城市内交互对象：

| 城市 | 对象 ID | 类型 | 第一轮 key | 后续事件 ID | 第二轮 key | 结果类型 | 下一步 | 中英校对 | 运行验证 |
|---|---|---|---|---|---|---|---|---|---|
| 大不里士 | `ev-tauris-a` | site | `ev.tauris.a.*` | `ev-tauris-a-merchants-followup` | `ev.ev_tauris_a_merchants_followup.*` | 情报/图鉴 | 揭示泰西封 | ✅ | ✅ |
| 待补 | `ev-...` | site/mentor | `ev....*` | `ev-...-followup` | `ev....*` | 物品/故事/情报 | 明确行动 | ☐ | ☐ |

P0 完成条件：12 主城 48 个城市交互对象逐行登记；至少 47 个缺失对象补上第二页；所有物品、故事和情报效果有对应文本和测试证据。

## 9. 验收标准

### 数据与文本

- [ ] P0 每个城市交互对象至少有两次可读互动；重要分支达到 2–4 个事件页；
- [ ] 每个选择至少满足“可见结果、后续事件、明确行动锚点”之一；
- [ ] 物品、故事、情报效果与结果文本逐项对齐；
- [ ] en/zh key 完整，无 raw key、无中英同文、无过期 stamp；
- [ ] 事件授予的 codex/sticker 条目必须同时有 name 与 body（G17 扩展 + `test_i18n_lines` §V）；`ui.culture.<band>` 与代码直接引用的 `ui.*` 字面 key 均有文本；
- [ ] 不使用无意义的通用反馈替代城市专属文本；
- [ ] 史料、改写、虚构文本的 `origin` 和 ref 正确。

### 运行时

- [ ] 选择后能看到结果页或自动打开下一事件；
- [ ] 连续事件可以关闭、暂停、保存、读档和恢复；
- [ ] 结果页关闭后城市探索点状态刷新；
- [ ] 获得物品、情报、图鉴、贴纸、声望和时间效果实际结算；
- [ ] 不出现点击后无文本、无事件、无行动的空选择。

### 验证命令

```bash
node tools/validate/validate.mjs --quiet
node tools/lore/story.mjs check
node tests/test_i18n_lines.mjs
godot --headless --path . --script tests/run_tests.gd
for test in tests/smoke_*.gd; do godot --headless --path . --script "$test" || exit 1; done
```

## 10. 交付顺序

1. ~~完成 P0 12 主城 site/mentor 多轮文本矩阵~~ ✅；
2. ~~先写英文源文，再完成中文行纪腔翻译与文化校对~~ ✅（12 主城 + 21 city）；
3. ~~接入 `queue_event`、effects 和结果文本~~ ✅；
4. ~~为每城增加真实 UI smoke~~ ✅ `smoke_21city_followups.gd` + 既有十二城/巴里黑 smoke；
5. ~~完成 24 法正式课程与新增 UI 文案~~ ✅（2026-08-06 T1：24 课 `lesson.*` 双语 + 16 非 MVP 占法 480 条结果文本 + 流程 UI `ui.*` 迁移）；
6. ~~再扩展 21 city~~ ✅ 2026-08-06（见 [`21_CITY_DEEPENING_MATRIX.md`](21_CITY_DEEPENING_MATRIX.md)、G31）；其他已有 site 质检见 §4.3；
7. 全量人工走读、存档恢复和发布候选验证。

本文件中状态为“未完成”的项目才进入当前文本生产；已归档批次不得重复开工，除非新增需求改变验收标准。
